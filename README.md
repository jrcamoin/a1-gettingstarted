Sample Readme (delete the above when you're ready to submit, and modify the text below with your links and descriptions)
---

Jacob Camoin
https://a1-jacobcamoin.onrender.com

This project shows ...

## Technical Achievements
- **Styled page with CSS**: Added rules for the body, h1, h2, p, li, and ul selectors...
    - html semantics:
        - I used `<header>` for the page title and introductory area. used `<main>` for the primary content on the page.
        - used `<article>` to group all the writing about me. `<section>` used this to organize the technology-related info.
        - `<footer>` was used to display information at the bottom of the page. `<ul>` and `<li>` for list related items
    - css style choices:
        - I used the body rule for the page background, Roboto font, text color. for headings - h1, h2, used these for hierarchy, used different colors for ultimate contrast. Each list item has its own contrasting background, unordered list has a dark background. Paragraphs have a separate text color.
- **Added JS animation**: used querySelectorAll() to animate every paragraph fading in when the page first loads

## Design Achievements
- **Used the Roboto Font from Google Fonts**: I used Roboto as the font for the primary copy text in my site.

- **Color choices** - created a color palette in adobe color and used the colors throughout the website
    - I wanted to style the page with contrasting and ugly colors. none of the colors really match and all of them clash
    - i used these colors: #e9eaed, #91D6B0, #d88715, #902543, #5e8282, #4f5b36, #38154b
    - all of these colors are quite different in base (with the starting color) - also, some are bright, dark, or neutral



